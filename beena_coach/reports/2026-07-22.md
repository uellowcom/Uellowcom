# تقرير المدرّب — بينا (Beena Coach)
**التاريخ:** 2026-07-22 (الأربعاء) · **المجال المُدقّق:** `uellow_smart_fit` (controllers + models)

## الملخّص
تمّت مراجعة كامل الوحدة استاتيكياً: `fit_controller.py`, `tryon_controller.py`, `tryon_extras.py`, `portal.py` والموديلات (`body_profile`, `product_size_chart`, `size_chart_template`, `tryon_session`, `fit_history`). الوحدة في حالة جيدة بعد تدقيقات الأيام السابقة (IDOR على body.profile وجلسات التجربة، سقف النتيجة عند 100، توجيه جدول المقاسات الحقيقي، التحقق من بايتات الصورة). عُثر على **خلل واحد حقيقي** جديد وتمّ إصلاحه.

## الخلل الذي عُثر عليه وأُصلح
### 1) تجاوز بوّابة الأهلية في التجربة الافتراضية (compose_outfit / compose_next)
- **الخطورة:** متوسطة (bug)
- **الوصف:** بوّابة الأهلية للتجربة الافتراضية (فئة فاشن + حد أدنى للسعر — قاعدة «التكلفة تستاهل») كانت مطبّقة فقط في `/tryon/generate`. أمّا نقطتا «مُنسّق الإطلالة» `/tryon/compose-outfit` و `/tryon/compose-outfit/next` فكانتا تقبلان أي `product_id` وتولّدان تجربة مدفوعة عبر FASHN لأي منتج (إلكترونيات… إلخ) متجاوزتين البوّابة والتكلفة.
- **الإصلاح:** أُضيف فحص `_product_eligible()` لكل قطعة في `compose_outfit` وللمنتج التالي `next_product_id` في `compose_next`، مع إرجاع نفس شكل الخطأ (`reason`/`info`) المستخدم في `generate()`. الميزة مغلقة افتراضياً (`outfit_composer=False`) لكن الإصلاح يمنع أي استنزاف تكلفة عند تفعيلها.
- **الكومِت:** `9db55a6`

## الحالة الصحية بعد النشر
- إعادة التشغيل مؤكّدة (تغيّر `StartedAt`).
- `/web/health` = **200** · `/shop` = **200**.
- بوّابة الصياغة (ast.parse) اجتازت قبل النشر.

## الدروس / السجلّات المضافة
- `beena.coach.finding` #1072 (category=bug, severity=medium, state=applied).
- لا حاجة لدرس جديد في `learned_lessons` (الخلل كودي بحت وتمّ إصلاحه مباشرة).

## يحتاج انتباه بشري
- **لا شيء حرِج.** ملاحظات منخفضة الخطورة لم تُغيَّر (تجنّباً للمخاطرة على الإنتاج):
  - `/fit/quick` و `/fit/analyze` (عامّة) قد تُرجع 500 عند إرسال قيمة عددية غير صالحة نصياً (مثل `chest="abc"`)؛ الحالات الفارغة/None محميّة مسبقاً. لا تُستدعى هكذا من واجهة بينا الحقيقية.
  - `_analyze_fit` (مسار التقدير الاحتياطي فقط) يعطي نتيجة محايدة 50 لكل المقاسات عند فئة «generic» أو مقاسات رقمية لا تطابق الجداول المضمّنة — مقبول لأنه موسوم كـ«تقديري» ويُستخدم فقط عند غياب جدول مقاسات المنتج.

---

## تدقيق ثانٍ (نفس اليوم) — تمريرة إضافية على `uellow_smart_fit`
أُعيدت مراجعة كامل الوحدة استاتيكياً مرّة أخرى (المتحكّمات + الموديلات). البوّابات الأمنية سليمة: كل نقاط التجربة الافتراضية تتحقّق من ملكية الصورة/الجلسة (`_require_image`, `partner_id == request.env.user.partner_id`)، ونقاط البورتال مُنطاقة بالـ partner، والكابات (شهري USD/عدد + يومي لكل عميل) مطبّقة. عُثر على **خلل واحد جديد** (منخفض الخطورة) وأُصلح.

### 2) انهيار (HTTP 500) في نقاط الفِت العامّة عند إدخال غير رقمي
- **الخطورة:** منخفضة (bug)
- **الوصف:** النقاط العامّة `/fit/analyze` و `/fit/quick` و `/fit/eligibility` و `/fit/check` كانت تحوّل `product_id` عبر `int()` والقياسات عبر `float()` دون أي حماية. أي قيمة غير رقمية (`chest="abc"`، `product_id="x"`) تُطلق `ValueError` → استجابة **500** على نقاط عامّة مكشوفة. أداة توصية المقاس في بينا قد تمرّر قياسات يكتبها المستخدم، فالاحتمال واقعي.
- **الإصلاح:** أُضيف مُحلّلان آمنان `SmartFitController._num()` و `_int_id()` ووُجّهت المواضع الأربعة عبرهما؛ الإدخال السيّئ يُعيد الآن خطأ JSON نظيفاً بدل 500. تغيير محصور وقابل للعكس بالكامل.
- **الكومِت:** `0732972`

### الحالة الصحية بعد التدقيق الثاني
- إعادة التشغيل مؤكّدة (تغيّر `StartedAt`: `00:03` → `04:02`).
- `/web/health` = **200** · `/shop` = **200**.
- بوّابة الصياغة (ast.parse) اجتازت قبل النشر.

### السجلّات المضافة
- `beena.coach.finding` #1073 (category=bug, severity=low, state=applied).
- لا حاجة لدرس جديد في `learned_lessons` (خلل كودي بحت وأُصلح مباشرة).

### يحتاج انتباه بشري
- **لا شيء حرِج.** المسار الاحتياطي التقديري في `_analyze_fit` (عند غياب جدول مقاسات المنتج) يعطي نتيجة محايدة للفئات «generic» — مقبول لأنه موسوم «تقديري». باقي الوحدة نظيف بعد تدقيقات الأيام السابقة.

---

## تدقيق ثالث (Pass 3) — 2026-07-22 08:03 UTC

**المنطقة:** `uellow_smart_fit` (المتحكّمات + الموديلات) — مراجعة معمّقة لتدفّقات التجربة الافتراضية (Try-On) وضوابط التكلفة.

### الخلل المُصلَح
**تجاوز سقف التكلفة عبر `/tryon/compose-outfit/next`**
- **الخطورة:** متوسطة (تحكّم بالتكلفة).
- **الوصف:** كل نقاط توليد الصور (`generate`, `regenerate_color`, `regenerate_background`, `compare_models`, `compose_outfit`) تستدعي `_check_caps(partner)` قبل صرف تكلفة المزوّد. لكن `compose_next()` — التي تُطلق توليد FASHN مدفوعاً جديداً لكل طبقة ملابس — لم تكن تفحص السقوف إطلاقاً. عميل مسجَّل يقدر يتجاوز **السقف الشهري بالدولار** و**سقف عدد التوليدات الشهري** و**السقف اليومي لكل عميل** عبر تكرار طبقات المنسّق، ما يفتح باب تكلفة غير محدودة على المزوّد.
- **الإصلاح:** أُضيفت بوّابة `_check_caps(partner)` في `compose_next` قبل استدعاء المزوّد، متطابقة مع باقي النقاط؛ تُعيد سبب/تفاصيل السقف عند تجاوزه. تغيير محصور وقابل للعكس. (الميزة `outfit_composer` مطفأة افتراضياً، فالتعرّض فعليّ فقط عند تفعيل الأدمن لها.)
- **الكومِت:** `3fc5ada`

### الحالة الصحية بعد التدقيق الثالث
- إعادة التشغيل مؤكّدة (تغيّر `StartedAt`: `04:02` → `08:03`).
- `/web/health` = **200** · `/shop` = **200**.
- بوّابة الصياغة (ast.parse) اجتازت قبل النشر.

### السجلّات المضافة
- `beena.coach.finding` #1074 (category=bug, severity=medium, state=applied).
- لا حاجة لدرس جديد في `learned_lessons` (خلل كودي بحت أُصلح مباشرة).

### يحتاج انتباه بشري
- **ملفّات غير مُلتزَمة لم أُنشئها ولم أمسّها:** يوجد متحكّم لوحة كوتش حيّة غير مُلتزَم `uellow_ai_engine/controllers/coach_live.py` مع تعديل على `controllers/__init__.py` يستورده، إضافة إلى `beena_coach/status` ومجلّدات `static/` جديدة. المتحكّم يكشف مسارات `/beena/coach/stats` و`/beena/coach/live` بـ `auth='public'` لكنها **محميّة بمفتاح** (`uellow_ai.coach_dashboard_key`) ويُرجِع 403 افتراضياً عند غياب المفتاح — آمن بالإعداد الافتراضي. تركتُه كما هو (لم أُنشئه) ولم أُدرجه في أي كومِت؛ يُنصَح بمراجعة بشرية لقرار الالتزام به أو حذفه.
- بقيّة الوحدة نظيفة: فحوصات الملكية (`_require_image`, ملكية الجلسة في `compose_next`) وقواعد السجلّات (`body_profile_own_rule`) والتحقّق من الصور وحدود الإدخال كلها سليمة بعد تدقيقات الأيام السابقة.

---

## تدقيق رابع (Pass 4) — 2026-07-22 09:04 UTC

**المنطقة:** `uellow_smart_fit` (المتحكّمات + الموديلات) — مراجعة معمّقة لمسارات رفع الصور والبورتال.

### الخلل المُصلَح
**رفع صورة التجربة عبر البورتال يتجاوز التحقّق من نوع/حجم الصورة**
- **الخطورة:** متوسطة (bug / تحكّم بالموارد).
- **الوصف:** نقطة `/my/photos/upload` في بورتال الوحدة كانت تخزّن أي ملف مرفوع مباشرةً في `customer.body.profile.tryon_photo` **دون أي فحص لبايتات الصورة (magic bytes) ولا حدّ أقصى للحجم** — على عكس مسار JSON `/tryon/upload-photo` الذي جرى تحصينه سابقاً (`_looks_like_image` + `_MAX_PHOTO_BYTES` = 8MB). النتيجة: يمكن تخزين بايتات غير صورة أو ملف ضخم في قاعدة البيانات، ثم يُرسَل لاحقاً إلى مزوّد التجربة المدفوع — إهدار موارد وتكلفة على حقلٍ يغذّي التوليد.
- **الإصلاح:** أُعيد استخدام `TryOnController._looks_like_image` و`_MAX_PHOTO_BYTES` داخل معالج البورتال؛ الملف غير الصورة أو الأكبر من 8MB يُرفَض بإعادة توجيه خطأ (`?error=invalid` / `?error=too_large`) قبل أي تخزين. تغيير محصور وقابل للعكس بالكامل.
- **الكومِت:** `350a651`

### الحالة الصحية بعد التدقيق الرابع
- إعادة التشغيل مؤكّدة (تغيّر `StartedAt`: `08:03` → `09:04`).
- `/web/health` = **200** · `/shop` = **200**.
- بوّابة الصياغة (ast.parse) اجتازت قبل النشر.

### السجلّات المضافة
- `beena.coach.finding` #1075 (category=bug, severity=medium, state=applied).
- لا حاجة لدرس جديد في `learned_lessons` (خلل كودي بحت أُصلح مباشرة).

### يحتاج انتباه بشري
- **ملفّات غير مُلتزَمة لم أُنشئها ولم أمسّها** (كما في التدقيق الثالث): `uellow_ai_engine/controllers/coach_live.py` + تعديل `controllers/__init__.py`، و`beena_coach/status`، ومجلّدات `static/` جديدة (`uellow_ops_ux/static/`, `delivery_carrier_portal/static/mockups/`). لم تُدرَج في أي كومِت — تحتاج قرار مراجعة بشرية.
- بقيّة الوحدة نظيفة: فحوصات الملكية على كل نقاط التجربة والبورتال والجلسات سليمة، الكابات مطبّقة على كل نقاط التوليد، سقف النتيجة ≤100، توجيه جدول المقاسات الحقيقي، والتحقّق من الإدخال الرقمي في النقاط العامّة — كلها من تدقيقات سابقة وما زالت سارية.

---

## تدقيق خامس (Pass 5) — 2026-07-22 (إعادة تدقيق نهائية)

**المنطقة:** `uellow_smart_fit` (المتحكّمات + الموديلات كاملةً).

أُعيدت قراءة كامل الوحدة سطراً بسطر: `fit_controller.py`, `tryon_controller.py`, `tryon_extras.py`, `portal.py`، والموديلات (`body_profile`, `tryon_session`, `product_size_chart`)، وملفّات الصلاحيات (`ir.model.access.csv`, `body_profile_rule.xml`).

### النتيجة: **لا أخطاء جديدة** (صفر)
تأكّدت سلامة كل ما يلي (نتاج تدقيقات اليوم السابقة، وما زال سارياً):
- **الملكية / IDOR:** كل نقاط التجربة الافتراضية تتحقّق عبر `_require_image` و`partner_id == request.env.user.partner_id` (`status`, `my_sessions`, `regenerate_color/background`, `compose_next`, `compare_models`, `reviewer_votes`). نقاط البورتال كلّها مُنطاقة بـ `request.env.user.partner_id`.
- **الصلاحيات:** موديلات `customer.fit.history` / `customer.tryon.session` / `customer.tryon.image` بلا صلاحية بورتال في ACL (وصول ORM المباشر مغلق للبورتال؛ المتحكّمات تستخدم `sudo` مع فحص ملكية يدوي). `customer.body.profile` مقيّد بقاعدة سجلّ `body_profile_own_rule` على `group_portal`.
- **ضبط التكلفة:** `_check_caps` (سقف شهري USD/عدد + يومي لكل عميل) مطبّق على **كل** نقاط التوليد بلا استثناء.
- **الأهلية:** `_product_eligible` (فئة فاشن + حدّ أدنى للسعر) مطبّقة في `generate` و`compose_outfit` و`compose_next`.
- **التحقّق من الصور:** `_looks_like_image` (magic bytes) + `_MAX_PHOTO_BYTES=8MB` على مساري الرفع (JSON `/tryon/upload-photo` وبورتال `/my/photos/upload`).
- **صلابة الإدخال:** `_num` و`_int_id` يحميان النقاط العامّة من الإدخال غير الرقمي؛ سقف النتيجة `min(score,100)`؛ توجيه جدول المقاسات الحقيقي عبر `_analyze_with_chart` مع وسم المسار الاحتياطي كـ«تقديري».
- **التحقّق الحيّ:** الحقول والموديلات موجودة فعلاً (`is_fashion_item`, `size_chart_ids`, `has_size_chart`, `customer.tryon.image/session`, `customer.body.profile`).

لم تُجرَ أي تعديلات كودية في هذا التمرير (لا حاجة لإعادة تشغيل).

### الحالة الصحية
- `/web/health` = **200** · `/shop` = **200**. الخدمة مستقرّة.

### السجلّات / الدروس
- لم يُنشَأ أي `beena.coach.finding` جديد (صفر أخطاء) ولا درس جديد في `learned_lessons` — تفادياً لتضخيم السجلّات دون داعٍ.

### يحتاج انتباه بشري
- **ملفّات غير مُلتزَمة لم أُنشئها ولم أمسّها** (كما في التمريرات السابقة): `uellow_ai_engine/controllers/coach_live.py` + تعديل `controllers/__init__.py`، و`beena_coach/status`، ومجلّدات `static/` جديدة (`uellow_ops_ux/static/`, `delivery_carrier_portal/static/mockups/`). تبقى خارج أي كومِت وتحتاج قرار مراجعة بشرية (الالتزام أو الحذف).

---

## تدقيق سادس (Pass 6) — 2026-07-22 11:23 UTC

**المنطقة (FOCUS OVERRIDE):** `uellow_ai_engine/controllers/ai_controller.py` — أدوات بينا (`_fn_*`) فقط.

### الخلل المُصلَح
**أداة `award_review_points` ميتة ومضلّلة (تعتمد على موديل غير موجود)**
- **الخطورة:** متوسطة (bug / hallucination).
- **الوصف:** الأداة كانت تعتمد على موديلَي `loyalty.account` و`loyalty.transaction` — وتأكّدت حيّاً أنهما **غير موجودين** في هذا التنصيب (نظام الولاء الحيّ هو `loyalty.card`). لذلك كانت الأداة تُرجِع دائماً `available:False`، بينما وصفها في `TOOLS` يعلن «Award 50 loyalty points»، ما يدفع بينا لوعد العميل بنقاط مراجعات لا تُمنَح أبداً. تأكيد حيّ من محادثة حقيقية `#254`: بينا سردت طرق كسب نقاط مُختلَقة (مراجعات + «200 نقطة لكل صديق» + «نقاط عيد الميلاد»).
- **الإصلاح:** أُزيلت الأداة من `TOOLS` و`TOOL_TRIGGERS` وخريطة التوزيع (`fn_map`)، وحُوِّل `_fn_award_review` إلى Stub أمين يوضّح أن نقاط المراجعات تُضاف تلقائياً — دون أي كود ضد موديل مفقود. تغيير محصور وقابل للعكس (git revert).
- **الكومِت:** `1aa7bac`

### تعدين المحادثات الحقيقية (Step 8) — أخطاء سلوكية مؤكّدة
دُقّقت ~44 ردّاً حديثاً لبينا (المحادثات 253–274). عُثر على خطأين سلوكيين حقيقيين وأُضيف لكلٍّ درس مختصر (مع فحص عدم التكرار):
1. **اختلاق توفّر/سعر تحت ضغط العميل (conv#270):** بعد إظهار «باور بانك Vidvie» غير متوفّر بشكل صحيح عدّة مرّات، وتحت إلحاح العميل، اختلقت بينا نسخة أخرى «Vidvie 8000mAh بسعر 7.0 KD متاحة للطلب المسبق حتى لو الستوك صفر» — معلومة كاذبة. → درس تحت **SAFETY & ANTI-HALLUCINATION**.
2. **اختلاق مزايا كسب نقاط ولاء (conv#254):** أرقام/مزايا غير مدعومة من النظام (دعوة أصدقاء 200 نقطة، نقاط عيد ميلاد). → درس تحت **LOYALTY PROGRAM**.
- `learned_lessons`: 23130 → 23838 حرفاً (أقل من سقف 40k).
- سجلّات: `beena.coach.finding` #1076 (bug), #1077 (hallucination/high), #1078 (hallucination/medium) — كلها `state=applied`.

### الحالة الصحية بعد النشر
- إعادة التشغيل مؤكّدة (تغيّر `StartedAt`: `09:04` → `11:23`).
- `/web/health` = **200** · `/shop` = **200**. بوّابة `ast.parse` اجتازت قبل النشر.

### يحتاج انتباه بشري
- **قرار منتج:** إن رُغِب فعلاً بمنح نقاط على المراجعات، يجب ربطها بنظام `loyalty.card` الحيّ **مع التحقّق من الشراء** (وإلا تُستغَلّ بمنح نقاط بلا شراء) — قرار بشري، لم أُنفّذه تلقائياً على الإنتاج.
- **مشكلة جلسة متكرّرة بعد تسجيل الدخول:** عدّة محادثات (`#261/#263/#264/#267`) يسجّل فيها العميل الدخول لكن بينا تبقى ترى الجلسة كضيف ولا تجلب الطلبات/السلة/النقاط («الجلسة ما اتزامنت»). المعالجة اللغوية سليمة، لكن يبدو أن `uid` جلسة الشات لا يُحدَّث بعد الدخول — يحتاج تحقيقاً بشرياً في تدفّق مزامنة الجلسة (خارج نطاق أدوات `_fn_*`).
- **ملفّات غير مُلتزَمة** (كما في التمريرات السابقة): `coach_live.py` + تعديل `__init__.py`، و`beena_coach/status`، ومجلّدات `static/` — تبقى خارج أي كومِت وتحتاج قرار مراجعة بشرية.

---

## تدقيق سابع (Pass 7) — 2026-07-22 (FOCUS: نماذج `uellow_ai_engine/models`)

**المنطقة (FOCUS OVERRIDE):** `ai_session` · `beena_conversation` · `beena_qa_cache` · `beena_dashboard` · `ai_config` · `voice_router` · `beena_order`.

### الأخطاء الكودية
**لا أخطاء جديدة في هذه المنطقة.** قُرِئت كل الملفات بالكامل وجرى التحقّق الحيّ:
- `ai.chat.session.get_or_create` يقصر البحث على `partner_id` (لا IDOR) — مثبّت من تمرير سابق.
- `voice_router`: التحقّق حيّاً أن `voice_enabled=True` و`voice_cache_enabled=True` في الإنتاج؛ كتابة `write_date` في `_cache_get` غير ضارّة (لا تُطلِق استثناءً — أُكِّد حيّاً)؛ سقوف الدقائق (`_check_caps`) مضبوطة (شهري=1000، لكل عميل=30) ولا التفاف عليها.
- `ai_config`: `default_get`/`execute` متماثلان ويُعيدان تحميل مفاتيح الـ API قبل الحفظ (لا مسح للمفاتيح) — «settings reset» مُصلَح من تمرير سابق.
- `beena_dashboard`: حقول الـ Html تستخدم `html_escape` (لا XSS) — مُصلَح سابقاً.
لم أُدخِل أي تعديل كودي ⇒ لا إعادة تشغيل، الخدمة لم تُمَسّ.

### تعدين المحادثات الحقيقية (Step 8)
دُقّقت ~40 ردّاً (المحادثات 239–274). أغلب الأنماط مغطّاة بدروس قائمة (هلوسة السعر/التوفّر تحت الضغط conv#270، اختلاق نقاط الولاء conv#254، حلقة «سجّل دخول» بعد الدخول — مغطّاة أصلاً تحت ESCALATION). عُثر على **خطأ سلوكي واحد جديد وغير مُغطّى**:
- **مبالغة ثم تناقض حول التجربة الافتراضية (conv#266):** أكّدت بينا «بالتأكيد! عندنا ميزة التجربة الافتراضية ✨» ثم ناقضت نفسها بعد ردٍّ واحد «هذه الميزة ما طُلقت بعد». الميزة (Phase 2) غير منجزة، ودروس القسم كانت تفترض إمكان استدعاء الأداة (بل قائمة SAFETY البيضاء ذكرت try-on كأداة متاحة). → أُضيف درس تحت **SIZE, FIT & VIRTUAL TRY-ON**: لا تؤكّدي الميزة كأنها فعّالة؛ قولي إنها «قريباً» واعرضي Smart Fit فوراً.
- `learned_lessons`: 23838 → 24175 حرفاً (أقل من سقف 40k).
- سجلّ: `beena.coach.finding` #1079 (hallucination/medium, `state=applied`).

### الحالة الصحية
- `/web/health` = **200** · `/shop` = **200**. لا إعادة تشغيل (لا تغيير كودي).

### يحتاج انتباه بشري
- **تعارض في التوجيه بشأن try-on:** درس SAFETY القائم يُدرِج «try-on» ضمن الأدوات المتاحة في القائمة البيضاء بينما الميزة غير مُطلَقة فعلياً — يُستحسن مواءمة هذا السطر لاحقاً حتى لا يعود التناقض (لم أعدّله تلقائياً تفادياً لإضعاف قاعدة أمان قائمة).
- **مشكلة مزامنة الجلسة بعد الدخول** (conv#261/263/264/267): بنية تحتية، لا تزال تحتاج تحقيقاً بشرياً (مذكورة في تمرير سابق).

---

## تدقيق ثامن (Pass 8) — 2026-07-22 (FOCUS: `uellow_smart_fit` controllers + models)

**المنطقة:** `fit_controller` · `tryon_controller` · `tryon_extras` · `portal` + كل الموديلات + ملفّات الأمان (ACL + ir.rule).

### الأخطاء المُكتشَفة والمُصلَحة
1. **[عالٍ/data-leak-fix] العملاء لا يستطيعون رؤية صور التجربة الافتراضية الخاصة بهم في البورتال.**
   - النموذجان `customer.tryon.image` و`customer.tryon.session` كان لهما ACL لمجموعة `base.group_user` (موظّفون داخليّون) فقط — **بلا ACL لـ`base.group_portal`**. النتيجة: وسم `<img src="/web/image/customer.tryon.image/<id>/image">` في صفحة `/my/tryon-history` (`portal_templates.xml:392`) يُعيد `AccessError` ويظهر كصورة مكسورة لكل عميل حقيقي استخدم الميزة (حيّاً: 17 جلسة / 22 صورة).
   - **تحقّق حيّ:** العميل البورتالي `Test@test.com` (partner 3911) يملك الصورة #17 لكن `check_access('read')` رمى `AccessError`.
   - **الإصلاح:** إضافة ACL للقراءة فقط لـ`base.group_portal` على النموذجين + قاعدتي `ir.rule` تحصران البورتال في سجلّاته `[('partner_id','=',user.partner_id.id)]`. تحقّق بعد النشر: المالك يقرأ صورته وجلسته ✅، عميل بورتالي آخر يُمنَع (`AccessError` — لا IDOR) ✅، الكتابة تبقى ممنوعة ✅.
   - **الكومِت:** `a7d1df5` — سجلّ `beena.coach.finding` #1080 (bug/high, `state=applied`).

### النشر والحالة الصحية
- نُشِر عبر `-u uellow_smart_fit` (تحميل ACL + XML بنجاح: «Modules loaded») ثم إعادة تشغيل — تغيّر `StartedAt`: `11:23:43` → `11:39:29`.
- `/web/health` = **200** · `/shop` = **200**. بوّابتا `xml.minidom` + التحميل اجتازتا قبل النشر.

### تعدين المحادثات الحقيقية (Step 8)
دُقّقت ~50 ردّاً (المحادثات 255–274). كل الأنماط المُلاحَظة مُغطّاة أصلاً بدروس قائمة: اختلاق نسخة/سعر تحت الضغط (conv#270)، وعد ثم تراجع عن التجربة الافتراضية (conv#266)، حلقة «سجّل دخول» بعد الدخول (conv#261/263/264/267 — بنية تحتية)، تحفّظ صحيح عن جهاز/آيفون غير موجود (conv#257/258/268/269). **لا خطأ سلوكي جديد غير مُغطّى ⇒ لم يُضَف أي درس** (لا حشو). `learned_lessons` يبقى 24175 حرفاً (أقل من سقف 40k).

### يحتاج انتباه بشري
- **تقادم درس التجربة الافتراضية:** الميزة الآن **مُفعّلة حيّاً** (`uellow_ai.tryon_enabled=True`، المزوّد الأساسي FASHN، 17 جلسة / 22 صورة)، بينما درس Pass 7 لا يزال يقول إنها «غير مُطلَقة بعد». قد يدفع هذا بينا لإنكار ميزة متاحة فعلاً. لم أُعدّل الدرس تلقائياً (تفادياً لإضعاف قاعدة سلوكية دون تأكيد تدفّق التوليد الكامل end-to-end) — يُستحسن أن يُواءم بشرياً بعد التحقّق من الميزة كاملةً.
- **مشكلة مزامنة الجلسة بعد تسجيل الدخول** (conv#261/263/264/267): بنية تحتية خارج نطاق `uellow_smart_fit` — تبقى بحاجة تحقيق بشري (مذكورة في تمريرات سابقة).

---

## تدقيق إضافي (Pass) — واجهة تطبيق الجوال `uellow_mobile_manager/controllers/api_v2`

**المنطقة المُدقّقة:** نقاط نهاية الـ API للجوال (v2) — الطلبات، العناوين، المحفظة، المراجعات، الضمان، الإشعارات، السلة.

### الخلل المُكتشف والمُصلَح
- **ثغرة IDOR في تأكيد الطلب (تسريب بيانات عميل آخر) — خطورة: عالية**
  - نقطة `POST /api/mobile/v2/orders/checkout/confirm` كانت تكتب `partner_shipping_id` / `partner_invoice_id` من `delivery_address_id` / `invoice_address_id` القادمة في الطلب **دون التحقق من ملكية العنوان**. عميل مُسجّل يقدر يوجّه طلبه الخاص إلى **أي `res.partner`** ثم يقرأ اسم/هاتف/شارع/مدينة الضحية عبر `GET /api/mobile/v2/orders/<id>` (تُسلسِل `delivery_address`).
  - **التحقق الحي:** طلب مسودة 6661 (الشريك 4) مقابل شريك ضحية 3785 باسم وهاتف حقيقيين — المسار قابل للاستغلال فعلاً.
  - **الإصلاح:** التحقق من أن العنوان يخص العميل الحالي (نفسه أو عنوان فرعي `parent_id`) أو أنه مرتبط مسبقاً بالسلة (حالة الضيف) قبل الإسناد — بنفس نمط الحماية الموجود أصلاً في `shipping_methods`.
  - **الالتزام (commit):** `d74f10b`
  - سُجِّل كـ `beena.coach.finding` رقم 1081.

### الصحة بعد النشر
- إعادة التشغيل: تغيّر `StartedAt` ✓ — الصحة `/web/health` = **200** ✓ — `/shop` = **200** ✓

### استخراج الدروس من محادثات حقيقية (40–55 رد)
- عُثر على سلوكيْن قابلين للتدريب (مناداة العميل باسم النظام «Developer»؛ ووعد بينا الزائف بميزة «التجربة الافتراضية» في conv#266 ثم تراجعها) — لكن **كلاهما مُغطّى بالفعل بدروس قائمة**، فلم أُضِف أي درس جديد (تفادياً للتكرار). طول `learned_lessons` = 24175 حرفاً (ضمن الحد).

### يحتاج انتباه بشري
- `POST /notifications/<id>/read` للإشعارات الشخصية (`mobile.customer.notification`) يضع `is_read=True` دون التحقق من ملكية الإشعار — لا تسريب بيانات، أثره ضعيف جداً (تغيير علامة "مقروء" فقط). تُركت بلا إصلاح تلقائي لأنها منخفضة الخطورة؛ يُنصح بإضافة فحص `partner_id` مستقبلاً.

---

## جولة إضافية — مرور Dropship + Vendor/Driver API (مسائي)

**المنطقة المدقّقة:** `uellow_dropship` + `uellow_vendor_api` + `uellow_multivendor` + `uellow_driver_api` — تركيز على ثغرات الملكية (IDOR).

### الأخطاء المُكتشفة والمُصلَحة (commit `c12d2f4`)
1. **IDOR في رحلات السائق — خطورة عالية.** `GET /api/driver/v1/trips/<id>` و`POST .../reorder` كانا يفتحان أي `trip_id` دون التأكد أن الرحلة تخص السائق — تسريب أسماء وعناوين عملاء كل محطات رحلة سائق آخر، وإمكانية إعادة ترتيب رحلة غيره. أُضيف فحص `_owns_trip` (driver_id / pickup_driver_id / محطة مُسندة للسائق).
2. **IDOR في تفاصيل تحصيل النقد — خطورة عالية.** `GET /api/driver/v1/cash/<id>` كان يفتح أي إيصال تحصيل دون التحقق من شركة النقل — تسريب إجماليات نقد وعمولة شركة نقل أخرى + أسماء العملاء. حُصِر على `driver.carrier_company_id` (نفس فلتر `history`).
3. **IDOR في روابط الدفع — خطورة عالية.** `generate/status/share/cancel` تحت `/api/driver/v1/orders/<id>/payment-link*` كانت تفتح أي `order_id`؛ سائق يملك صلاحية `can_send_payment_link` كان يقدر يولّد/يلغي رابط دفع لأي طلب ويقرأ اسم وهاتف العميل. أُضيف فحص `_driver_owns_order` (delivery_driver_id أو محطة رحلة مُسندة).

> `uellow_vendor_api/orders.py` و`returns.py` مضبوطان بالكامل (كل نقطة تفحص `vendor_id == v.id`) — لا خلل.

### النشر والصحة
- بوابة `ast` نجحت للملفات الثلاثة، أُعيد تشغيل الحاوية (StartedAt تغيّر إلى `11:51:15`)، **الصحة = 200**، و`/shop = 200`.
- سُجّلت 3 نتائج `beena.coach.finding` (bug/data، high، state=applied).

### استخراج الدروس من محادثات حقيقية (~50 رد، conv#253–274)
- خطآن سلوكيان حقيقيان: (أ) conv#270 — بينا رضخت لعميل ملحّ واخترعت «Vidvie 8000mAh قابل للطلب المسبق حتى لو المخزون صفر» متناقضةً مع قولها الصحيح قبلها؛ (ب) conv#261/263/264/267 — حلقة «سجّل الدخول» تتكرر 3–4 مرات دون تصعيد رغم أن العميل أكّد دخوله وأعطى رقم طلب S78956.
- **كلا الخطأين مُغطّى بدروس قائمة** (درس مكافحة الهلوسة يذكر conv#270 بالاسم؛ وقسم التصعيد يفرض التصعيد بعد فشلين) — فلم أُضِف أي درس جديد تفادياً للتكرار. طول `learned_lessons` = 24175 حرفاً (ضمن الحد).
- سُجّلت نتيجة `ux` واحدة توثّق أن حلقة الدخول **فجوة تطبيق** لدرس قائم (يُقترح فرض التصعيد برمجياً في `ai_controller` بعد فشلين لأدوات جلسة الزائر).

### يحتاج انتباه بشري
- حلقة عدم مزامنة الجلسة بعد تسجيل الدخول (conv#261/263/264/267) تكرّرت في محادثات حقيقية متعددة — الأثر تجربة مستخدم سيئة وإحباط عملاء غاضبين. الإصلاح سلوكي/برمجي في تدفق `ai_controller` (تصعيد إجباري بدل تكرار رابط الدخول) وليس ثغرة أمنية؛ تُرك للمراجعة البشرية لأنه يمسّ منطق المحادثة الأساسي.

---

## تمريرة إضافية — تدقيق المدفوعات (2026-07-22)

**المنطقة المدقّقة:** `odoo_upayments` + `uellow_upayments` + `uellow_checkout` + `uellow_bnpl` + `ms_payment_cod` + `delivery_carrier_portal` — تركيز على ثغرات الملكية (IDOR) وتسريب بيانات العملاء.

### الأخطاء المُكتشفة والمُصلَحة
1. **تسريب PII / IDOR في صفحة نجاح الطلب — خطورة حرجة (commit `25dbc82`).** المسار العام `/shop/order/success?order_id=N` كان `auth='public'` ويفتح **أي** طلب ويعرض اسم العميل وعنوانه الكامل ومدينته و**رقم هاتفه** وأصناف الطلب والأسعار والإجمالي. أي زائر مجهول يقدر يعدّاد أرقام الطلبات ويحصد بيانات كل العملاء. أُضيف فحص ملكية `_uc_may_view_order` (صاحب الحساب المسجّل / الطلب المؤكَّد في نفس الجلسة عبر `_uc_remember_order` / سلة الجلسة / `access_token` صالح)، وإلا تُعرض صفحة تأكيد عامة بلا أي بيانات. تم التحقق: جلب مجهول لطلب حقيقي = 0 مؤشر تسريب مع بقاء صفحة «تم تأكيد الطلب».
2. **IDOR عبر شركات النقل في بوابة الموصّلين — خطورة عالية (commit `a02afd3`).** `receive_order` و`no_receive_order` كانا يفحصان `_is_manager` فقط دون التأكد أن الطلب يخص شركة النقل الخاصة بالمدير (بعكس أشقائهما `mark_returned`/`assign_driver`). مدير شركة نقل A كان يقدر يمرّر `order_id` لشركة B ويغيّر حالة التوصيل (`arrived_sorting`/`failed_returned`) ويكتب على سطر رحلة عبر `line_id`. أُضيف فحص `order.delivery_carrier_company_id == company` وحُصِر تحديث السطر على الطلب نفسه.

> بقية الوحدات مضبوطة: `uellow_upayments` (webhook يعيد التحقق من الحالة عبر API ولا يثق بـ`result` المُرسَل)، `uellow_bnpl/apply` (فحص `commercial_partner_id`)، `ms_payment_cod`، `odoo_upayments`، وباقي نقاط `delivery_carrier_portal` (فحوص `_owned_order`/الشركة موجودة).

### النشر والصحة
- بوابة `ast` نجحت للملفين، أُعيد تشغيل الحاوية مرتين (StartedAt تغيّر)، **الصحة = 200**، و`/shop = 200`.
- سُجّلت نتيجتا `beena.coach.finding` (bug — critical/high، state=applied).

### استخراج الدروس من محادثات حقيقية (~50 رد، conv#256–274)
- أخطاء سلوكية حقيقية رُصدت لكنها **كلها مُغطّاة بدروس قائمة**: حلقة عدم مزامنة الجلسة بعد الدخول (conv#261/263/264/267)، وتسرّب لفظة «Developer» في رد قائمة الطلبات (conv#256 — مُغطّى بدرسَي «لا تستخدم أسماء نائبة مثل Developer»)، واختلاق منتج للطلب المسبق تحت الإلحاح (conv#270 المذكور بالاسم في قسم مكافحة الهلوسة).
- لم أُضِف أي درس جديد (تفادي التكرار). طول `learned_lessons` = 24175 حرفاً (ضمن حد الـ40k).

### يحتاج انتباه بشري
- **`save_address` في `uellow_checkout`** يطابق العميل بالهاتف فقط ثم يكتب فوقه (الاسم/الشارع/المدينة/الإيميل) دون تحقق ملكية — نظرياً يقدر زائر يعرف هاتف عميل آخر يعدّل بياناته المخزّنة. لم أُصلحه تلقائياً لأنه سلوك دمج-الضيوف المقصود في المتجر، وتضييقه قد يكسر تدفّق الضيوف المتكرّرين أو يُنشئ سجلات مكرّرة — يُترك للمراجعة البشرية (يُقترح على الأقل عدم الكتابة فوق `email` لسجل قائم).

---

## تدقيق إضافي (باس مسائي) — SEO + الريفيوّرز + مراجعات المنتجات

**المنطقة:** `uellow_seo_manager` + `uellow_seo_auto` + `uellow_reviewers` + `product_reviews`

### الأخطاء المُكتشفة والمُصلَحة
- **[عالٍ] IDOR في `/reviewers/accept`** (`uellow_reviewers/controllers/reviewer_controller.py`): كان المسار `auth='user'` يجلب `review.request` برقمه بـ`sudo` ويستدعي `action_accept`/`action_expire` **دون التحقق أن الطلب يخصّ ريفيور المستخدم الحالي**، ثم يُرجع `req.token`. أي مستخدم مسجّل يقدر يقبل/يُنهي طلب ريفيور آخر ويستخرج التوكن السرّي (الذي يفتح إرسال الرسائل وتقديم الرأي عبر المسارات العامة المعتمدة على التوكن). **الإصلاح:** التحقق أن `req.reviewer_id` يساوي `reviewer.profile` الخاص بالشريك الحالي قبل أي إجراء (نفس نمط الحارس في واجهة الموبايل `reviewer_api`). كوميت `a247574`.

- بقية المسارات فُحِصت وهي سليمة: `quick_verdict` و`reviewer_api` تتحققان من الملكية؛ `/uellow/seo/health` محميّ بـ`group_system`؛ `robots.txt`/`sitemap-pages.xml` قراءة عامة فقط؛ صفحة الهبوط والتقديم للريفيوّرز سليمة.

### الحالة الصحّية بعد النشر
- إعادة التشغيل مؤكدة (تغيّر StartedAt) — `/web/health` = **200** — `/shop` = **200**.

### درس/تعلّم من محادثات حقيقية (خطوة 8 — عيّنة 55 ردّاً)
- **[أُضيف]** تناقض حالة التوفر داخل نفس المحادثة (conv#259: عُرِض Vidvie Wireless 20000mAh «متوفر» ثم «غير متوفر» عند الإضافة) → درس تحت PRODUCT DISCOVERY.
- **[أُضيف]** وعد بتتبّع الطلب برقمه لعميل غير مسجّل ثم رفضه (conv#267: طُلِب S78956 ثم أُعيد التوجيه لتسجيل الدخول) → درس تحت ORDER MANAGEMENT.
- طول `learned_lessons` بعد الإضافة = **24712** حرفاً (ضمن حد 40k). findings مُسجّلة: 1088 (IDOR), 1089, 1090.
- بقية العيّنة: بينا رفضت بشكل صحيح المنتجات الوهمية (جهاز التنقل X9، iPhone 20 Pro Max) ولم تختلق أسعاراً — سلوك سليم.

### يحتاج انتباه بشري
- المسارات العامة المعتمدة على التوكن (`/reviewers/verdict`, `/reviewers/message`, `/reviewers/rate`) تعتمد على سرّية توكن `review.request` (uuid مقصوص 12 حرفاً) بدل تحقق ملكية صريح — مقبول ما دام التوكن لا يُسرَّب (وقد أُغلق تسريبه عبر إصلاح `/reviewers/accept`)، لكن يُفضَّل مستقبلاً ربط إجراءات الريفيور بجلسة المستخدم.
