<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_vendor_location_list" model="ir.ui.view">
        <field name="name">uellow.vendor.location.list</field>
        <field name="model">uellow.vendor.location</field>
        <field name="arch" type="xml">
            <list string="Vendor Sub-Warehouses"
                  decoration-muted="state!='active'">
                <field name="partner_id"/>
                <field name="location_name"/>
                <field name="units_stored"/>
                <field name="restock_count"/>
                <field name="storage_fee"/>
                <field name="state" widget="badge"
                       decoration-success="state=='active'"
                       decoration-warning="state=='suspended'"
                       decoration-danger="state=='closed'"/>
                <button name="action_view_stock" string="View Stock"
                        type="object" icon="fa-cubes"/>
            </list>
        </field>
    </record>

    <record id="view_vendor_location_form" model="ir.ui.view">
        <field name="name">uellow.vendor.location.form</field>
        <field name="model">uellow.vendor.location</field>
        <field name="arch" type="xml">
            <form string="Vendor Sub-warehouse">
                <header>
                    <button name="action_suspend" string="تعليق"
                            type="object" invisible="state != 'active'"/>
                    <button name="action_activate" string="تفعيل"
                            type="object" class="oe_highlight"
                            invisible="state == 'active'"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <group>
                        <group string="التاجر">
                            <field name="partner_id" options="{'no_create': True}"/>
                            <field name="location_id" readonly="1"/>
                            <field name="location_name" readonly="1"/>
                        </group>
                        <group string="شروط التخزين">
                            <field name="storage_fee"/>
                            <field name="max_days"/>
                            <field name="units_stored" readonly="1"/>
                            <field name="restock_count" readonly="1"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_vendor_location" model="ir.actions.act_window">
        <field name="name">مخازن التجار الفرعية</field>
        <field name="res_model">uellow.vendor.location</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
