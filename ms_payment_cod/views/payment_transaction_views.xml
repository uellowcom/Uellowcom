<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="payment_transaction_search" model="ir.ui.view">
        <field name="name">payment.transaction.search</field>
        <field name="model">payment.transaction</field>
        <field name="inherit_id" ref="payment.payment_transaction_search"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='partner_name']" position="after">
                <filter string="COD Pending" name="cod_pending" domain="[('provider_id.custom_mode', '=', 'cod'), ('state', '=', 'pending')]"/>
                <filter string="COD Collected" name="cod_collected" domain="[('provider_id.custom_mode', '=', 'cod'), ('state', '=', 'done')]"/>
            </xpath>

        </field>
    </record>

    <record id="payment_transaction_form" model="ir.ui.view">
        <field name="name">payment.transaction.form</field>
        <field name="model">payment.transaction</field>
        <field name="inherit_id" ref="payment.payment_transaction_form"/>
        <field name="arch" type="xml">

            <xpath expr="//button[@name='action_void']" position="after">
                <field name="custom_mode" invisible="1"/>
                <button name="action_collect" type="object" string="Collect Payment" class="oe_highlight" invisible="custom_mode != 'cod' or state != 'pending'"/>
            </xpath>

        </field>
    </record>

</odoo>
