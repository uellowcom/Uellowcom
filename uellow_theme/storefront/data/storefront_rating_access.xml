<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Grant public users read access to rating.rating records -->
        <!-- This fixes the 403 on product pages for guests -->
        <record id="rule_rating_public_read" model="ir.rule">
            <field name="name">Rating: public read</field>
            <field name="model_id" ref="rating.model_rating_rating"/>
            <field name="domain_force">[(1,'=',1)]</field>
            <field name="groups" eval="[(4, ref('base.group_public'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>
    </data>
</odoo>
